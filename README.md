# UPSC Tracker — Upgraded

Features:
- Editable rows & checkboxes
- Autosave to localStorage
- Dark mode toggle
- Excel import/export (SheetJS)
- Firebase placeholders (Auth + Firestore) - add your config
- PWA manifest + service worker

Quick start:
1. npm install
2. npm run dev

Firebase:
- Create a Firebase project and add web app.
- Copy config and paste into src/firebaseConfig.js

PWA:
- App registers a simple service worker for offline shell.

